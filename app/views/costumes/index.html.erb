<div class="container-fluid">
      <div class="row">
        <div class="col-md-8">
          <p></p>
          <h1><strong>Search Costumes By:</strong></h1>
          <form class="" action="/costumes">
            <input class="form-control mr-sm-2 mb-2" type="text" name="search[city]" placeholder="Enter City" aria-label="Search">
            <input class="form-control mr-sm-2 mb-2" type="text" name="search[tag]" placeholder="Enter Category" aria-label="Search">
            <div class="d-flex align-items-center">
              <div class="check-box mr-5">
                <p>Gender</p>
                <% ["male", "female", "unisex"].each do |gender| %>
                <label for="<%= gender %>"><%= gender %></label>
                <input hidden type="checkbox" value="<%= gender %>" id="<%= gender %>" name="search[gender]">
                <% end %>
              </div>
              <div class="check-box mr-5">
                <p>Size</p>
                <% ["x-small", "small", "medium", "large", "x-large"].each do |size| %>
                <label for="<%= size %>"><%= size %></label>
                <input hidden type="checkbox" value="<%= size %>" id="<%= size %>" name="search[size]">
                <% end %>
              </div>
              <button class="btn btn-outline-success my-2 my-sm-0 align-self-end" type="submit">Search</button>
            </div>
          </form>
        </div>
      </div>
      <div class="row">
        <div class="col-md-8">
          <% @costumes.each do |costume| %>
          <% if costume.photos.attached? %>
          <div class="col-md-12">
            <div class="costume-card card m-4 p-3 flex-row">
              <div class="row w-100">
                <div class="col-md-6">
                  <%= cl_image_tag costume.photos.first.key, heigth: 200, class: "card-img-top"%>
                </div>
                <div class="col-md-6">
                  <div class="card-body">
                    <h5 class="card-title"><%= link_to costume.name, costume_path(costume)  %></h5>
                    <p class="card-text"><%= costume.description %></p>
                    <% if policy(costume).edit? %>
                    <%= link_to "Edit the costume", edit_costume_path(costume) %>
                    <% end %>
                    <% if policy(costume).destroy? %>
                    <%= link_to "Destroy the costume", costume_path(costume), method: :delete, data: { confirm: "Delete #{costume.name}?"} %>
                    <% end %>
                    <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <% end %>
          <% end %>
        </div>
        <div class="col-md-4">
          <div id="map"
          style="width: 100%; height: 100vh; top:60px"
          class= "position-fixed"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>
  </div>